# ================================
# 📦 DATABASE CONFIGURATION
# ================================

# Nome do serviço do banco no docker-compose (não use "localhost")
DB_HOST=db

# Porta padrão do PostgreSQL
DB_PORT=5432

# Usuário do banco de dados
DB_USERNAME=rural_admin

# Senha do banco de dados (altere para produção)
DB_PASSWORD=SuaSenhaSuperSegura456!

# Nome do banco de dados a ser criado
DB_DATABASE=rural_producer_db

# ================================
# 🚀 APP CONFIGURATION
# ================================

# Porta em que a aplicação irá rodar
PORT=3000

# Ambiente da aplicação (production, development, test)
NODE_ENV=production

# ================================
# ⚡ REDIS CONFIGURATION
# ================================

# Nome do serviço Redis no docker-compose
REDIS_HOST=redis

# Porta padrão do Redis
REDIS_PORT=6379

# URL local de conexão com o Redis (caso precise rodar localmente fora do Docker)
REDIS_URL=redis://127.0.0.1:6379

# ================================
# 🔐 JWT CONFIGURATION
# ================================

# Chave secreta para geração de tokens JWT
JWT_SECRET=ProjetoTesteNode